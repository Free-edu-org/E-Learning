# For production (Azure), VITE_API_BASE_URL must be an empty string to use relative paths
VITE_API_BASE_URL=""
# BACKEND_URL is used by the Nginx proxy in the production container
BACKEND_URL=https://freeedu-backend.[your-id].[region].azurecontainerapps.io
