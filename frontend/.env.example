# For local development: http://localhost:8080
# For production (Azure): "" (empty string to use relative paths)
VITE_API_BASE_URL=http://localhost:8080
# BACKEND_URL is used by Nginx in the production container
BACKEND_URL=http://backend:8080
